<h1 class='slogan'>
    @if (session('current_language') == 'en')
        <span>{{ __('dropar') }}</span>... master classe!!
    @else
        A <span>dropar</span>... master classe!!
    @endif
</h1>